name: Pillownote
options:
  bundleIdPrefix: com.zzoutuo
  deploymentTarget:
    iOS: "17.0"
  createIntermediateGroups: true
settings:
  base:
    SWIFT_VERSION: "5.9"
    MARKETING_VERSION: "1.0"
    CURRENT_PROJECT_VERSION: "1"
    DEVELOPMENT_TEAM: JP4TN5PTS3
    CODE_SIGN_STYLE: Automatic
    CODE_SIGN_IDENTITY: "Apple Development"
targets:
  Pillownote:
    type: application
    platform: iOS
    sources:
      - path: Pillownote
      - path: Shared
    dependencies:
      - target: PillownoteWidgets
    entitlements:
      path: Pillownote/Pillownote.entitlements
      properties:
        com.apple.security.application-groups:
          - group.com.zzoutuo.Pillownote
        com.apple.developer.icloud-container-identifiers:
          - iCloud.com.zzoutuo.Pillownote
        com.apple.developer.icloud-services:
          - CloudKit
        aps-environment: development
    info:
      path: Pillownote/Info.plist
      properties:
        CFBundleDisplayName: Pillownote
        CFBundleShortVersionString: "1.0"
        CFBundleVersion: "1"
        UILaunchScreen: {}
        UIApplicationSceneManifest:
          UIApplicationSupportsMultipleScenes: false
        UISupportedInterfaceOrientations:
          - UIInterfaceOrientationPortrait
          - UIInterfaceOrientationPortraitUpsideDown
          - UIInterfaceOrientationLandscapeLeft
          - UIInterfaceOrientationLandscapeRight
        NSMicrophoneUsageDescription: "Pillownote uses the microphone to record voice notes for your partner."
        NSSpeechRecognitionUsageDescription: "Pillownote transcribes your voice notes so they can be read as text."
        NSCameraUsageDescription: "Pillownote uses the camera to attach photos to your pillow notes."
        NSPhotoLibraryUsageDescription: "Pillownote lets you attach photos from your library to pillow notes."
        NSUserNotificationsUsageDescription: "Pillownote sends reminders for daily questions and note arrivals."
        ITSAppUsesNonExemptEncryption: false
        NSAppTransportSecurity:
          NSAllowsArbitraryLoads: false
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.zzoutuo.Pillownote
        TARGETED_DEVICE_FAMILY: "1,2"
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        ASSETCATALOG_COMPILER_GLOBAL_ACCENT_COLOR_NAME: AccentColor
  PillownoteWidgets:
    type: app-extension
    platform: iOS
    sources:
      - path: PillownoteWidgets
      - path: Shared
    entitlements:
      path: PillownoteWidgets/PillownoteWidgets.entitlements
      properties:
        com.apple.security.application-groups:
          - group.com.zzoutuo.Pillownote
    info:
      path: PillownoteWidgets/Info.plist
      properties:
        CFBundleDisplayName: Pillownote Widgets
        NSExtension:
          NSExtensionPointIdentifier: com.apple.widgetkit-extension
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.zzoutuo.Pillownote.Widgets
        SKIP_INSTALL: YES
        TARGETED_DEVICE_FAMILY: "1,2"
schemes:
  Pillownote:
    build:
      targets:
        Pillownote: all
        PillownoteWidgets: [compile]
    run:
      config: Debug
      storeKitConfiguration: Pillownote.storekit
